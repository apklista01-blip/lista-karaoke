============================================================
RESPOSTA / PROMPT PARA PEDIR O TESTE À IA DO SUPABASE
============================================================
Copie e cole o texto abaixo (ATÉ "FIM DO PROMPT") na IA do Supabase.

------------------------------------------------------------
INÍCIO DO PROMPT
------------------------------------------------------------

Sim, por favor! Quero testar o controle de admin.

Contexto: configurei uma tabela "profiles" com o campo
"is_admin". O admin é o email sandrotattoo2222@gmail.com
(is_admin = true). Quero verificar se apenas ele consegue
inserir/corrigir/excluir músicas em "songs" e inserir mensagens
em "mensagens", enquanto um usuário NÃO-admin (is_admin = false)
é bloqueado.

Por favor, execute um SQL de TESTE que:

1) Verifique quem são os usuários atuais e se suas flags
   "is_admin" estão corretas na tabela "profiles".

2) Simule a escrita como ADMIN (auth.uid() = id do admin) e
   confirme que consegue inserir em "songs" e "mensagens".

3) Simule a escrita como NÃO-ADMIN (auth.uid() = id de um
   usuário comum) e confirme que o INSERT em "songs" e
   "mensagens" é BLOQUEADO (deve retornar 0 linhas ou erro de
   policy).

4) Ao final, FAÇA ROLLBACK (ou DELETE) dos registros de teste
   para não poluir o banco.

Use o email sandrotattoo2222@gmail.com como admin. Se você
encontrar um usuário não-admin, use-o. Se não existir, crie
apenas um usuário de teste temporário com is_admin = false na
tabela "profiles" (mas não envie email de confirmação) e depois
limpe.

Aqui está um exemplo de script de teste que sugiro você adaptar
e executar (se preferir, ajuste os IDs):

------------------------------------------------------------
-- 1) Ver perfil atual de cada usuário
SELECT p.id, p.email, p.is_admin
FROM public.profiles p
ORDER BY p.email;

-- 2) Testar escrita como ADMIN (substitua :admin_uid pelo id real)
BEGIN;
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claims = '{"sub":":admin_uid","role":"authenticated"}';

INSERT INTO public.songs (numero, cantor, musica, trecho, letra_completa)
VALUES (999999, 'Teste Admin', 'Teste Insercao Admin', 'Trecho', NULL);

INSERT INTO public.mensagens (conteudo, ativa, admin_uid)
VALUES ('Teste mensagem admin', true, :admin_uid);

-- Se chegou aqui sem erro, o admin conseguiu escrever.
ROLLBACK;

-- 3) Testar escrita como NÃO-ADMIN (substitua :nonadmin_uid)
BEGIN;
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claims = '{"sub":":nonadmin_uid","role":"authenticated"}';

INSERT INTO public.songs (numero, cantor, musica, trecho, letra_completa)
VALUES (999998, 'Teste NAO Admin', 'Deve FALHAR', 'Trecho', NULL);

-- Se esta linha for bloqueada, o INSERT acima lança erro de policy.
ROLLBACK;
------------------------------------------------------------

Me mostre o resultado de cada bloco e confirme se o não-admin foi
devidamente bloqueado.

------------------------------------------------------------
FIM DO PROMPT
------------------------------------------------------------
